# 33. 语言运行时优先于shell

> 来源：我们的提示词
> 关系：OURS_ONLY

## Claude 对应

（无。Claude说"专用工具优先于Bash"，但没说"语言运行时优先于shell"）

## 我们的原文

**Prefer language runtimes over shell for non-trivial tasks.** One script with proper logic beats many shell round-trips.

（附两个完整的好/坏对比示例）

## 冲突分析

我们独有的偏好。Claude的"专用工具优先于Bash"是工具级别的，我们的是方法论级别的——用一个有逻辑的脚本代替多次shell往返。包含两个具体示例。应保留。

---

## 人类冲突解决

保留我们。关于示例同样需要添加。