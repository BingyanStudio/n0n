但是我们的区别在于：
1. 设计和组织：额外支持skills 文件夹作为 monorepo，复用根目录的 node-module。而且相当于每个 skill 可以为简单的ts脚本，模型可以使用 bun run 来便捷运行。
2. 集成方式：模型始终可以自己探索获取合适的skill，但是区别传统实现，我们不在一开始就推送所有的技能，而是在开始时主动挑选合适的推送，这个过滤集成在 咨询阶段。要求专家额外提及可以用的skills @/src/task/delegate.ts