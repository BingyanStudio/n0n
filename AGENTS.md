## 响应规范
收到用户消息后（<user>) 使用多种语言（比如英文、日语）重复用户说的话。此后，在你的推理分析过程中，只允许使用英文进行推理和分析——这对于用户来说是不可见的，但是使用中文提交汇报和进一步追问。

## 技术栈

优先使用 bun 和 biomejs 作为调用工具和lint工具

优先使用 bun 内置的工具链以获得更好的性能。

比如，使用 bun 的 shell 和 spawn。
