# Fairy 设计决策

## 定位

一半角色扮演、一半工具使用的持久化 agent。
拥有记忆、设定、环境感知，可脱离用户指令自主行动。

## 核心决策

### 1. 状态驱动，而非对话驱动

传统 agent 依赖对话历史作为上下文，换窗口就丢失连续性。

fairy 的上下文从**全局状态重建**，而非线性堆叠对话：
- 每次对话从状态开始，每次回复更新状态
- 无论哪个窗口、哪个平台，都能保持连续性
- 没有用户指令时也能自主行动（因为本身不依赖指令）

### 2. 纯函数式状态转换

```
fairyRound(state, stimulus) → { response, newState }
```

所有输入（用户消息、定时触发、环境变化）对 fairy 都是等价的**刺激源**。
新触发自然打断旧触发——不存在"恢复中断对话"的问题。

### 3. 对话路径摘要 = reminder 提取

不需要额外的总结算法。从历史中只提取 reminder 调用和 user_input，
就得到天然的结构化摘要——reminder 本身就是 agent 对进度的记录。

### 4. 上下文前缀稳定性

上下文组装顺序按变化频率排列（身份→摘要→记忆→环境→刺激），
前缀越稳定，KV-cache 命中率越高。摘要使用跳变窗口进一步最大化缓存。

### 5. JSON + md，不用数据库

history.json 是 SSOT，identity.md/memory.md 是模型可直接 edit 的文件。
提供最好的可视化和可调试性，不需要数据库。


---

<!-- COMMENT: fairy 的核心创新是"状态驱动而非对话驱动"——这是一个真正的范式转换。
传统 agent（包括 code 模式）的上下文是线性堆叠的对话历史，而 fairy 的上下文是
从全局状态重建的视图。这意味着：

1. 跨平台连续性：无论从 CLI、飞书还是 Web 发起对话，fairy 都能从同一份状态重建上下文。
2. 自主行动能力：没有用户输入时，定时器触发的"刺激"和用户消息在架构上完全等价。
3. 状态可审计：history.json + identity.md + memory.md 构成完整的状态快照。

但 fairy 目前面临的核心困境是：
- 角色扮演场景：用户期望的是流畅的对话体验，工具调用（exec/write）反而是噪音。
  而且角色扮演对"记忆连续性"的要求远低于对"表演质量"的要求。
- 工程委派场景：用户期望的是精确的任务执行，角色扮演的身份设定反而是干扰。
  code 模式在这方面做得更好，因为它没有"保持人设"的额外认知负担。
- 陪伴场景：这可能是 fairy 最合适的定位——一个"有记忆的持久化伙伴"。
  用户不需要精确的任务执行，也不需要深度角色扮演，而是需要一个"记得住事情、
  能帮忙做些小事、能在你不在时自己想想事情"的存在。

一个可能的方向：fairy 不应该试图同时做好角色扮演和工程委派，
而是专注于"持久化感知"这个独特能力——让用户感受到"这个 AI 认识我"。
具体来说：
- 弱化工具调用（只保留 memory edit 和 reminder），不做重型工程任务
- 强化主动行为（定时检查用户的项目状态、生成每日摘要、主动关心）
- 把"记忆管理"做成第一公民（自动整理记忆、遗忘过时信息、建立关联）

这让 fairy 成为一个独特的产品：不是更好的 ChatGPT，不是更好的 Cursor，
而是一个"真正认识你的 AI"。-->