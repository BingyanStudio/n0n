/**
 * 飞书卡片模块 — 统一导出
 */

export {
	buildProcessCard,
	buildTextCard,
	chunkText,
	type LogLine,
	type RoundBlock,
} from "./builder.ts";
export type { CardHeaderTemplate, FeishuCardContent } from "./types.ts";
